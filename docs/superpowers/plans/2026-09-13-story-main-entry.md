# Story main entry implementation plan

Goal: make the implemented school story reachable through New/Continue and return from its final record without modifying story arithmetic or old saves. Inline execution under current user continuous-work approval. Scope is menu integration, not final art/settings/encyclopedia completion.

Delivery: all menu entry steps implemented. 12 runners1054 assertions, legacy54suites2153 assertions, Python20 tests0failures; validator CURRENT. Runtime4372 main→continue→final→main and new→cancel verified, screenshot story-main-entry-20260913.png, diagnostics0/0. Final return is runtime evidence rather than a dedicated headless assertion. Settings/catalog and in-activity menu navigation remain separate tasks. Current project main setting changed through editor; only one semantic line of project.godot changed.

Review passes: (1) actual source has no new-story menu, so wrapper selected, other PRs not absorbed; (2) corrupt-only save blocks replacement and bytes retained, non-directory ancestor fails before engine mkdir error; (3) StoryScreen ready originally replaced supplied state, corrected to initialize only empty; (4) main-setting dependency scan found 3 old main assertions and adapter drift, updated main ownership while preserving legacy tests; (5) related/full legacy tests, source/readback and runtime cancellation passed, visual/mobile/export and merge remain unverified. Base changes are observational drift only; pin retained. No claim of complete game or final menu art.

Spec owner: human-blueprint sections 10,14,32; shared-spell-rules-design and story_flow own gameplay. Baseline55f941b5, main d384c454, other four draft PRs read-only. Base validator9.4.3 CURRENT. Existing project.godot modification has no semantic diff; preserve all other lines and old scene.

Research 2026-09-13: Ren'Py official screen_actions describes Continue loading last save and confirmation for destructive transitions (https://www.renpy.org/doc/html/screen_actions.html); Heaven's Vault official page describes story remembering choices (https://www.inklestudios.com/heavensvault/). ADAPT safe start/resume and preserved outcome; REJECT copying engine, story or UI art. Sources do not prove our save reliability or player UX.

Architecture: story_menu.gd owns menu view, validated load and new-game confirmation only. Existing Store owns two-slot persistence. New start saves fresh stage0 before entering; failure stays in menu. With corrupt slots and no valid payload, fail closed and preserve files. Existing StoryScreen accepts valid preloaded state, retains direct-scene fresh start, signals final-record main return. No RNG refresh on menu navigation. Story menu uses same explicit development save folder; export path migration is separate. Functional buttons/text only; no candidate art promoted.

- [ ] RED tests/run_story_menu_tests.gd: missing menu, invalid continue remains menu, new start stage0 persists, continue exact stage, new request with valid save awaits confirmation, cancellation preserves bytes/state, confirmed replacement becomes stage0, final-return signal, failed save remains menu, corrupt files not overwritten. Use dedicated project artifacts test paths, no production fixture changes.
- [ ] Implement src/ui/story/story_menu.gd/.tscn: methods request_new(), confirm_new(), cancel_new(), continue_story(), show_menu(); Store validation per action (no stale cached payload). new_pending gates confirm. View labels explain disabled Continue and incomplete Settings/Catalog. Story instance property save_folder before add_child. StoryScreen _ready initializes only empty state; final record button emits main_requested.
- [ ] Run new menu+story UI/flow/save; inspect exact editor11900 identity before scene authoring. Save new scene through editor; update project main setting only after tests. Capture actual menu→continue and new confirmation cancellation; retain existing local story record during runtime QA.
- [ ] Readback changed main setting, full affected runners and diagnostics, update Active Context/START_HERE, commit/push current branch and compare HEAD. No direct main push/other PR merge.

Five review passes cover entire scope and claims: (1) source/authority vs unrelated PR, (2) valid/corrupt/absent save transitions and replacement confirmation, (3) embedded story state ownership and exact resume, (4) actual layout/input and retained old scene, (5) regression/remote/unfinished settings-art-mobile evidence. No new paid tool, asset family or game meaning. Revert new entry path to old UID for rollback without deleting saves.

## Loop2: read-only school spell encyclopedia

Delivered: model15/menu17 and adjacent regressions612 assertions0failures. Runtime30916 open/scroll-view screenshot/back/continue0errors0warnings. Five full-scope reviews: learning gate (stage0 locked); discovery (preview excluded); record trust (mismatched ID RED fixed); read-only lifecycle (100 model reads and saved generation unchanged); UI/evidence (scroll footer visible, text-only no visual/Human claims). No history schema change or additional generated reward. UI record content is representative use, not inferred mastery. Numeric semantics remain in existing domain.

Spec Blueprint14/26: four learned glyphs, discovered combinations, representative actual use; no new reward, progress/RNG mutation or recipe spoilers. Research Magicka official https://www.paradoxinteractive.com/games/magicka/about ADAPT spell composition as experimentation, REJECT elemental/5-input mechanics; Heaven's Vault official https://www.inklestudios.com/heavensvault/ ADAPT connecting glyph learning and narrative, REJECT its language/guessing rules. Observations from official search extracts, not direct gameplay benchmarking.

Create src/core/shared_spell/story_codex.gd: build(story:Dictionary)->Dictionary validates via Flow; learned four glyphs from actual learned event state, discovered spells only existing cast receipts (including no-change or damage; label as use, never mastery/success). Event records provide spell name, action and target/context. Duel receipts provide spell identity; no reconstructing from current hand. No new save fields. Return independent presentation rows, invalid story returns locked rows/no leaked records. Terms from Semantics.RECIPES; Korean concise meanings are presentation text not numeric owner. Before a lesson, entries remain locked with explanation.

Menu open_codex reads Store without writing; show scrollable glyph meanings and used combinations/contexts, return main. Settings remains disabled. Tests tests/run_story_codex_tests.gd cover missing component RED, prelesson locked, learned without cast no combo reveal, actual lesson cast adds context, input deep equal before/after100 reads, malformed input stays locked. Menu tests assert codex return leaves saved generation/payload unchanged. Actual live saved story opens codex and back→continue. No imagery generation/approval promotion; runtime glyph visual still pending.

## Loop3: actual dialogue text setting

Delivered: preferences9/menu20/storyscreen22/storyflow27/codex15/eventsave12=105 assertions0failures. Actual runtime7640 setting32→continue body32, scroll area+buttons visible; restore24. Five reviews: scope explicitly dialogue only; independent save payload/RNG invariant; corruption fallback tested; live large text scroll preserves choices; stale main-completion copy corrected and final rerun clean. Full XAG101 all-important-text coverage, mobile and export remain NOT_RUN. Existing two-slot algorithm reused rather than another storage abstraction. Main/setting/codex code stays unmerged feature branch.

Bounded first settings consumer: dialog body only, 24/28/32px, default24. Label clearly '대화 본문 글자 크기'; no claim of scaling event/duel UI. Blueprint14's remaining audio/reduced motion/instant text persist as separate requirements. XAG101 text readability guidance supports user-adjustable text; not device certification.

Create story_preferences.gd with load_size(folder)->int and save_size(folder,size)->bool. Independent preferences.cfg under existing development save folder; ConfigFile whitelist sizes and validation; no story/RNG/slot changes, invalid config defaults24, invalid write value rejected. Menu open_settings and choose_text_size show three buttons and result; render does not write. StoryScreen reads setting when building dialogue and applies font size to body Label. Existing body ScrollContainer retains choices below it at1280x720. Store failure leaves old saved setting effective, no false 'applied' message.

Preimplementation correction: direct ConfigFile would truncate the previous preference on interrupted writes. Reuse existing EventSave two-slot envelope specialized to font_size, in separate `preferences/` subdirectory. No new persistence algorithm. Corrupt preference falls back to prior slot or24 if neither valid. Test wrong-type payload rejection and corrupt slot fallback instead of ConfigFile text parsing. XAG101 recommends all important text inherit accessibility scaling; this first body-only setting explicitly does NOT satisfy that full guideline and remains partial coverage.

RED tests/run_story_preferences_tests.gd: missing module, default/valid reload/invalid rejection/config wrong type fallback; menu test setting changes persisted size without changing story save generation; story screen test actual body font32 under valid saved preference and same story. Apply only current development folder, no outside-project writes or old-save migration. After RED/GREEN actual editor menu→settings32→continue check scroll/choices; return24 for representative runtime. Re-run related UI/store suites and diagnostics, update current docs. Scope excludes global font manager/animation systems/assets.

## Loop4: in-play menu preserves uncast selection

Pause implementation first readback: 12 assertions, zero failures. Full regression/runtime still pending.

## Dialogue presentation follow-up (latest user direction)

Replace status-report presentation with location, short narration, named speaker, dialogue and explicit choices. Reuse existing GrimoireThemeFactory navy/brass functional theme; no candidate art promotion. Separate production caveats into this development record, preserve actual outcome facts and failure notices. Official ink documentation https://www.inklestudios.com/ink/ ADAPT prose plus explicit narrative choices; REJECT new scripting-engine migration for this presentation change. Tests must detect separate speaker/narration and prohibit development-status prose; existing result/branch tests remain. No rule/save-schema change. First pass addresses story dialogue; event help/preview prose and final illustrated frame remain follow-up, not falsely complete.

Latest user reiterates entire-game completion, not stopping after isolated features. Next bounded connection: open menu from dialogue/event/duel, read codex/change dialogue settings and return to exact same screen instance. Do not rebuild activity or save UI draft as executed gameplay.

Keep one suspended_story child hidden with PROCESS_MODE_DISABLED; menu `_clear` preserves only this child, resume restores same instance/process/visibility and applies dialogue font without rerendering choices. StoryScreen emits pause_requested from dialogue title and forwards story_menu_requested from event/duel header buttons. Existing standalone screens show no story menu button. Menu shows '플레이로 돌아가기', settings, codex, explicit '진행 저장 후 메인으로'; latter explains uncast selections are reset, only leaves after successful Store save. StoryScreen.save_story returns bool while keeping previous callers compatible. Read codex from suspended current story, not possibly older disk save. New game cannot start while suspended. No clock tick, cast or draw occurs from any menu operation.

RED tests/run_story_pause_tests.gd: missing open_pause; lesson select EMBER+target→pause→codex→back→resume preserves same instance/selection/quote/state and no new save generation; actual cast after resume increments once; duel select pair→same cycle preserves hand/quote; save+main failure keeps suspended instance. Existing main/ending and standalone regressions rerun. Live editor exact project menu header and PC viewport check; mobile/native touch NOT_RUN. Lifecycle clears retained child only on explicit save+main or root cleanup. No new dependencies or art.
